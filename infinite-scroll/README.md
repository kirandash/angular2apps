# InfiniteScroll

ng new infinite-scroll

npm install ngx-infinite-scroll --save

npm install --save firebase angularfire2

ng g s image

Rules

{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}